{
  "name": "passport-verify",
  "private": true,
  "description": "MVP monorepo — run scripts from root or cd into frontend/backend/contracts/mobile",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:clean": "npm run dev:clean --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "npm run start --prefix backend",
    "contracts:compile": "npm run compile --prefix contracts",
    "mobile": "npm run start --prefix mobile"
  }
}
